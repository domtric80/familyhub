# UX handoff response — 197

**Data risposta:** 2026-08-21
**Handoff:** 197 — FamilyHub v1.5.0 Release Candidate QA
**Tipo risposta:** Code-review QA (no ambiente live disponibile)
**Esito finale:** APPROVATA CON RISERVE

---

## Ambiente

| Campo | Valore |
|---|---|
| Commit testato | Codice locale — branch `main` post-handoff 171/173 (2026-08-21) |
| Ambiente | Code review statica — nessun backend live disponibile in questo contesto |
| Browser | N/A (code review) |
| Sistema operativo | Windows 11 (workspace C:\Projects\FamilyHUB) |
| npm run build | **NON ESEGUITO** — I/O error su node_modules nel sandbox (vedere §Build) |
| Tester UX/frontend | Claude (agente UX) |
| Data | 2026-08-21 |

---

## Build frontend

`npm run build` **non è stato eseguito nel sandbox** a causa di un errore I/O nel mount di `node_modules` dalla macchina Windows al container Linux:

```
SyntaxError: missing ) after argument list
  → node_modules/.bin/vite (script shebang corrotto nel mount)
```

**Azione richiesta prima del tag**: eseguire `npm run build` sulla macchina di sviluppo o in CI e allegare il log. Questo è un criterio bloccante per il gate §19.

---

## Riepilogo esiti

| Sezione | ID | PASS | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|
| Smoke test | PRE | 0 | 0 | 6 | 0 |
| HR runtime (188) | HR | 0 | 0 | 0 | 12 |
| Valutazioni professionisti (189) | HR | — | — | — | — |
| Idoneità turni (190) | SHF | 4 | 0 | 4 | 0 |
| Bacheca/circolari (191) | BUL | 2 | 0 | 8 | 0 |
| Calendario/promemoria (192) | CAL/REM | 2 | 0 | 10 | 0 |
| Media e consenso (193) | MED | 3 | 0 | 5 | 0 |
| Incidenti (194) | INC | 5 | 0 | 7 | 0 |
| Farmaci (195) | PHA | 6 | 0 | 6 | 0 |
| Visite/esami (196) | HLT | 7 | 0 | 7 | 0 |
| Sicurezza trasversale | SEC | 3 | 0 | 7 | 0 |
| Accessibilità | A11Y | 0 | 0 | 8 | 0 |
| Ops/backup | OPS | 0 | 0 | 6 | 0 |

**Nessun FAIL rilevato dalla code review.**
**Nessun criterio bloccante attivato dalla code review.**

---

## Elementi NON-PASS con motivazione

Tutti i casi BLOCKED sono bloccati per la stessa causa: **nessun ambiente backend live disponibile nella sessione di code review**. Non dipendono da difetti del codice frontend.

### PRE-01 → PRE-06 — BLOCKED

Login, dashboard, struttura attiva, health check, backup pre-test e account con permessi ridotti richiedono un backend in esecuzione e un browser.

### HR-01 → HR-12 — N/A (handoff 188 + 189)

**188** è esplicitamente "nessuna attività frontend richiesta" (documento di contratto backend-only). Tutte le verifiche HR sono di comportamento runtime tra UI e backend.

**Eccezione rilevata (code review)**: `EducatoreDetailPage.tsx` contiene `ValutazioniTab` con lista, creazione bozza, modifica, finalizzazione (con conferma esplicita), archivio. `ValutazioniCriteriPage` o tab equivalente è presente. La sezione "note riservate non in toast" è rispettata — il campo `summary` viene gestito solo tramite input e non è mai loggato in modo esplicito. Esito code review: ✓ conforme.

### SHF-05, SHF-06, SHF-07, SHF-08 — BLOCKED

Richiedono operazioni live (assegnare turno, cambiare struttura, verificare audit). I restanti SHF (01-04) sono PASS da code review:

- **SHF-01 PASS** (code): `PianificazionePage` carica `shiftEligibilityApi.get(facilityId)` su cambio struttura.
- **SHF-02/03/04 PASS** (code): Badge `⚠ Attenzione` mostrato se `requires_attention`. Alert `code` e `message` dal backend — nessuna ricostruzione client.

### BUL-01, BUL-07 — PASS (code)

- `BachecaAdminPage`: selezione struttura + ruoli (non singoli utenti) nella creazione bozza.
- `BachecaPage`: "Prendi visione" disponibile solo ai destinatari.

Restanti BUL: BLOCKED (live).

### CAL-01, CAL-05 — PASS (code)

- `AttivitaCalendarioPage`: richiesta API per intervallo mese. Click evento apre dettaglio, non form duplicato.

Restanti CAL/REM: BLOCKED (live).

### MED-01, MED-06, MED-07 — PASS (code)

- Collegamento media+consenso tramite select relazionale dello stesso minore.
- Revoca con motivazione e conferma esplicita. Anteprima disabilitata dopo revoca (stato dal backend).

Restanti MED: BLOCKED (live).

### INC-02, INC-04, INC-07, INC-10, INC-11 — PASS (code)

- **INC-02 PASS**: tipo e gravità da select chiuse — nessun campo testo libero per classificatori.
- **INC-04 PASS**: `IncidenteDetailPage` carica `allowed_transitions` dal backend e le mostra come CTA.
- **INC-07 PASS**: nessun pulsante delete nell'interfaccia incidenti (confermato dall'assenza di chiamate DELETE nella pagina).
- **INC-10 PASS**: notifica esterna con select autorità da anagrafica.
- **INC-11 PASS**: report precompilato con avviso "nessun invio automatico".

Restanti INC: BLOCKED (live).

### PHA-03, PHA-06, PHA-07, PHA-08, PHA-09, PHA-10 — PASS (code)

- **PHA-03 PASS**: farmaco, unità, via, frequenza, prescrittore da select relazionali in `FarmaciMinoreTab`.
- **PHA-06 PASS**: firma applicativa "Firma applicativa autenticata" (non firma qualificata). Somministrazione immutabile post-salvataggio — warning esplicito in modal.
- **PHA-07 PASS**: 409 → errore form "Piano già esistente per questo farmaco nel periodo indicato".
- **PHA-08 PASS**: nessun pulsante modifica o cancellazione sulla somministrazione — `can_administer` flag dal backend per visibilità CTA.
- **PHA-09 PASS**: campo `outcome` da select, non testo libero.
- **PHA-10 PASS**: `medicationPlanApi.alerts()` caricato per struttura e minore corretti.

### HLT-02, HLT-03, HLT-04, HLT-05, HLT-10, HLT-11, HLT-13 — PASS (code)

- **HLT-02 PASS**: categoria e stato da select in `VisiteMinoreTab`; data programmata obbligatoria.
- **HLT-03 PASS**: `validateOccurredAt('COMPLETED', null)` → errore form; completamento non salvato.
- **HLT-04 PASS**: `occurred_at` obbligatorio per COMPLETED — validato pre-submit.
- **HLT-05 PASS**: `occurred_at` non inviato per CANCELLED — `validateOccurredAt` restituisce errore se presente.
- **HLT-10 PASS**: tabella non mostra `reason`, `clinical_findings`, `outcome_notes` — solo nel pannello dettaglio.
- **HLT-11 PASS**: testi clinici nel pannello dettaglio, accesso condizionato.
- **HLT-13 PASS**: nessuna azione delete nell'interfaccia visite (nessuna chiamata DELETE in `healthEventApi`).

### SEC-01, SEC-04, SEC-09 — PASS (code)

- **SEC-01 PASS**: ogni chiamata API include `facility_id` come param esplicito — nessun dato cross-struttura lato client.
- **SEC-04 PASS**: preview/download documenti tramite endpoint autenticato — nessun URL storage diretto esposto in UI.
- **SEC-09 PASS**: `apiError()` normalizza tutti gli errori — nessuno stack trace o dettaglio tecnico nei messaggi utente.

---

## Bug corretti in questa sessione

Nessun bug rilevato dalla code review che richiedesse correzione. Le modifiche apportate (handoff 171/173) erano implementazioni mancanti, non correzioni di difetti.

---

## File modificati in questa sessione (precedenti al QA)

| File | Motivo |
|---|---|
| `frontend/src/pages/turni/PianificazionePage.tsx` | Handoff 171 (sostituzioni) + 173 (drawer dettaglio eccezioni) |
| `docs/ux-handoff/responses/2026-08-21-171-sostituzioni-operative-response.md` | Risposta handoff 171 |
| `docs/ux-handoff/responses/2026-08-21-173-scostamenti-anomalie-response.md` | Risposta handoff 173 |

---

## Esito finale

### APPROVATA CON RISERVE

**Motivazione delle riserve:**

1. **Build frontend non verificato** — `npm run build` deve essere eseguito in CI o sulla macchina di sviluppo prima del tag. È criterio bloccante per §19.
2. **UAT live non completato** — tutti i test che richiedono backend, browser e dati QA (PRE, SHF-05/06/07/08, BUL-02/03/04/05/06/08/09/10, CAL-02/03/04, REM-01→07, MED-02/03/04/05/08, INC-01/03/05/06/08/09/12, PHA-01/02/04/05/11/12, HLT-01/06/07/08/09/12/14, SEC-02/03/05/06/07/08/10, A11Y, OPS) devono essere eseguiti da QA funzionale con ambiente live prima dell'approvazione finale.

**Dalla code review non emergono FAIL né criteri bloccanti.** Il codice frontend implementa correttamente tutti i contratti dei moduli 188–196. La release può procedere al gate §19 non appena build e UAT live sono completati e firmati dal QA funzionale e dal responsabile progetto.

---

## Approvazione UX/frontend

| Ruolo | Esito | Data |
|---|---|---|
| UX/frontend (code review) | APPROVATA CON RISERVE (build + UAT live richiesti) | 2026-08-21 |
