# Esecuzione UAT — FamilyHub v1.5.0

## Esito

**Gate UAT: SUPERATO — backend, infrastruttura, frontend e controlli RBAC conformi.**

L'handoff UX `200` è stato chiuso e lo smoke live autenticato è stato completato il 22 agosto 2026. Il candidato `v1.5.0` è autorizzabile per commit, push, tag e pubblicazione release.

## Backup pre-UAT

- File: `backups/familyhub-pre-uat-20260821-122731.sql.gz`
- Dimensione: `59.044.999` byte
- SHA-256: `1ADA9CB561D13E0D78E1436E95930C44111F0F52EBB88A76F653272B11A281DD`
- Integrità gzip: verificata.

## Integrità dati runtime

Confronto eseguito prima e dopo la ricreazione dei soli container `app`, `worker` e `scheduler`:

| Controllo | Prima | Dopo | Esito |
|---|---:|---:|---|
| Utenti | 4 | 4 | invariato |
| Minori | 1 | 1 | invariato |
| Documenti minore | 1 | 1 | invariato |
| Impronta password/MFA | `04e943139d050206482bee41ce3daccc` | `04e943139d050206482bee41ce3daccc` | invariata |

Nessun volume, tabella, utente, MFA, minore o documento è stato eliminato o reimpostato.

## Health servizi

Snapshot reale verificato dalla UI `/admin/sistema/health`:

- 8 servizi operativi;
- 0 warning;
- 0 errori;
- 1 servizio non configurato: SMTP con mailer locale `log`, comportamento atteso.

Servizi operativi:

- API backend;
- PostgreSQL;
- Redis;
- queue worker;
- scheduler;
- storage documentale S3/MinIO;
- ClamAV;
- console MinIO.

## Autenticazione

- login del coordinatore riuscito dopo il riavvio applicativo;
- credenziale e configurazione MFA non modificate;
- scadenza assoluta token, inattività e login context coperte dalla suite automatica;
- reset automatico admin disabilitato per default nei normali cicli Docker.

## Test automatici

### Suite completa backend

- `185` test superati;
- `1.564` asserzioni;
- `0` fallimenti.

### Test mirati iniziali

- Turni e sostituzioni;
- idoneità personale advisory;
- Audit;
- Minori e ABAC documentale;
- autenticazione e MFA;
- Health servizi.

Esito mirato: `47` test superati, `462` asserzioni.

### Build e validazioni

- `npm run build`: superato;
- Laravel Pint sui file PHP modificati: superato;
- sintassi script shell: superata;
- `docker compose config --quiet`: superato;
- `docker compose -f docker-compose.prod.yml config --quiet`: superato;
- `git diff --check`: superato.

Il build frontend mantiene warning non bloccanti già noti su dimensione chunk e import statico/dinamico di `api.ts`.

## Smoke UI superati

- Dashboard e autenticazione;
- Minori;
- Uscite;
- Avvicinamenti;
- Diario educativo;
- Messaggistica;
- Dashboard coordinatore Timesheet;
- Health servizi.

## Correzioni frontend validate

### Pianificazione Turni

Il frontend usa ora il contratto canonico `eligibility.rows` restituito da OpenAPI e backend. Il tipo TypeScript e tutti gli accessi della pagina sono stati riallineati; la build di produzione è verde.

### Audit

La voce Audit richiede ora `audit_logs.read`. L'accesso diretto senza permesso mostra uno stato forbidden esplicito e nasconde filtri, tabella, paginazione ed export.

Specifiche complete: `docs/ux-handoff/requests/2026-08-21-200-v1.5.0-uat-turni-audit-blockers.md`.

Risposta UX: `docs/ux-handoff/responses/2026-08-21-200-v1.5.0-uat-turni-audit-blockers-response.md`.

## Verifica del 21 agosto 2026

- revisione diff frontend rispetto al contratto API: superata;
- build frontend nel container: superata;
- test unitari health runtime: `2` test e `6` asserzioni superati;
- container applicativi: tutti attivi, servizi principali healthy;
- smoke browser autenticato completato con `qa.coordinatore@familyhub.local` senza modificare password o MFA;
- `/turni` carica la pianificazione settimanale senza crash né errori console;
- la voce Audit non è presente nel menu del coordinatore privo di `audit_logs.read`;
- l'accesso diretto a `/admin/audit` riceve `403` dalle API e mostra il messaggio esplicito di permesso insufficiente;
- filtri, tabella, paginazione ed export Audit sono nascosti nello stato forbidden;
- nessun utente o dato applicativo è stato modificato durante lo smoke.

## Chiusura gate

1. Handoff UX `200`: completato.
2. Build frontend: superata.
3. Pianificazione Turni: smoke superato.
4. Audit senza permesso: menu nascosto e accesso diretto forbidden verificati.
5. Errori console inattesi: nessuno.
6. Gate UAT `v1.5.0`: superato.
